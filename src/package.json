{
  "name": "load-service",
  "version": "1.0.0",
  "description": "Parcel load service",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cross-env NODE_ENV=dev node index.js ",
    "start:nodemon": "cross-env NODE_ENV=dev nodemon index.js",
    "debug": "cross-env NODE_ENV=dev nodemon --inspect index.js"
  },
  "author": "Satpal",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^8.2.0"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "compression": "^1.7.4",
    "config": "^3.3.3",
    "cross-env": "^7.0.3",
    "express": "^4.17.1",
    "express-joi-validation": "^5.0.0",
    "helmet": "^4.2.0",
    "joi": "^17.3.0",
    "mongoose": "^5.11.4",
    "morgan": "^1.10.0",
    "uuid": "^8.3.1"
  }
}
